import asyncio

async def run_orderflow():
    print("Order Flow kuzatuvchi modul ishladi...")
    # Bu yerda on-chain eventlar, Mempool, Volume imbalance kuzatiladi.
    # Keyinchalik bu ma’lumot asosida AI qaror qabul qiladi.
